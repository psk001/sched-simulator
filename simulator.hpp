#ifndef SIMULATOR_H
#define SIMULATOR_H

#include "scheduler.hpp"

class Simulator{
    public:   
        Simulator();
        Simulator(int, int, int);
       // Simulator(std::string, int, int);    
};

#endif 